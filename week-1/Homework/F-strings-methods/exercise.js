// Start by creating a variable `message`

const name='Joao';
const nameLength= name.length;

const message=`Hello, my name is ${name} and my name is ${nameLength} characters long`;

console.log(message);
