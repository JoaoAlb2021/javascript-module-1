const numberOfStudents = 15;
const numberOfMentors = 8;

const students = `Number of Students: ${numberOfStudents}`;
const mentors = `Number of Mentors: ${numberOfMentors}`;
const total = `Total number of students and mentors: ${numberOfMentors + numberOfStudents}`
console.log (students);
console.log (mentors);
console.log(total);