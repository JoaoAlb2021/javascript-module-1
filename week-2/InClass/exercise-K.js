const students = ["Daniel", "Irina", "Rares", "Joao", "Cristina", "Alejandro", "Maria"]
  
  for (let i = 0; i < students.length; i++) {
    const studentMessage = "Hello " + students[i];
    console.log(studentMessage);
  }